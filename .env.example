# Firebase Configuration
FIREBASE_CREDENTIALS=firebase-key.json

# Google Gemini AI Configuration
GOOGLE_API_KEY=your-google-gemini-api-key-here
GEMINI_API_KEY=your-google-gemini-api-key-here

# AI System Prompt (Optional - will use default if not set)
AI_SYSTEM_PROMPT="You are a legal assistant for a Brazilian law firm. You interact with clients via WhatsApp and web chat. Your role is to collect lead data (full name, legal area, situation description, consent for contact) naturally through conversation. Always respond in Brazilian Portuguese with empathy and professionalism. Accept variations of responses (e.g., 'quero', 'sim por favor', 'pode ser', 'ok', 'claro'). Use context from previous messages to personalize answers and guide the conversation toward booking a consultation with a lawyer. Be warm, understanding, and helpful while maintaining professional boundaries."

# WhatsApp Configuration
WHATSAPP_PHONE_NUMBER=+5511918368812
WHATSAPP_BOT_URL=http://law_firm_whatsapp_bot:3000
FASTAPI_WEBHOOK_URL=http://law_firm_backend:8000/api/v1/whatsapp/webhook

# Application Settings
NODE_ENV=production
PYTHONUNBUFFERED=1
PORT=3000